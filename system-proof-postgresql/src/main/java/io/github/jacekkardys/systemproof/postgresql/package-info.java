/**
 * Bounded plaintext PostgreSQL v3 observation for the characterized pgJDBC subset.
 *
 * <p>The package supplies exact commit-unit control, session-scoped transaction identity,
 * secret-safe typed evidence, an ephemeral write-correlation seam, and independent durability
 * prerequisite verification. It is not a general PostgreSQL proxy, TLS terminator, SQL engine, or
 * AML attribution layer.
 */
package io.github.jacekkardys.systemproof.postgresql;
