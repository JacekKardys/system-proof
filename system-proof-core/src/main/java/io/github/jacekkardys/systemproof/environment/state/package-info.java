/**
 * Defines detached immutable environment and runtime-connection state for inspection.
 *
 * <p>These values own no lifecycle, resources, endpoint values, or mutation capability. Journal
 * and diagnostics contracts may depend on this package without depending on environment execution.
 */
package io.github.jacekkardys.systemproof.environment.state;
