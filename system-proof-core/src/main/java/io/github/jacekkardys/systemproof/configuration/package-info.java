/**
 * Defines typed external configuration contracts, providers, immutable values, and redacted
 * secrets.
 *
 * <p>The binder, validator, and validation-value helper are Java-public only so package-private
 * environment assembly can cross the package boundary. They are unsupported implementation API.
 */
package io.github.jacekkardys.systemproof.configuration;
