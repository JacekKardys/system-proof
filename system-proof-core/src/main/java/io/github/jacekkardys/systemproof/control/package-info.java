/**
 * Defines environment-scoped, protocol-neutral semantic traffic controls.
 *
 * <p>The supported API arms one-shot typed holds before a stimulus and exposes only immutable
 * selector metadata, opaque identities, lifecycle state, and completion signals. Original bytes,
 * sockets, mutable buffers, executors, and protocol-specific policy remain outside this package.
 */
package io.github.jacekkardys.systemproof.control;
