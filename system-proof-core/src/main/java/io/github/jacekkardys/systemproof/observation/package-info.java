/**
 * Defines immutable interaction identity and evidence plus connection-scoped observation and
 * forwarding-decision capabilities.
 *
 * <p>These contracts are independent of journal storage. A journal records observation values but
 * does not own their schemas, session identities, or stream ordinals.
 */
package io.github.jacekkardys.systemproof.observation;
